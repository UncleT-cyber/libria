"""UI package: decoupled view routing and customtkinter pages.

Only :mod:`libria.ui.router` is import-safe without a display; ``pages`` and
``app`` import tkinter/customtkinter and must be loaded by the entry point.
"""
